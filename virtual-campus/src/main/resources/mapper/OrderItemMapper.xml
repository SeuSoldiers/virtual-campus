<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" 
    "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="seu.virtualcampus.mapper.OrderItemMapper">
    
    <!-- 批量插入订单项 -->
    <insert id="insertBatch" parameterType="java.util.List">
        INSERT INTO order_item(itemId, orderId, quantity, productId, unitPrice, subtotal)
        VALUES
        <foreach collection="list" item="item" separator=",">
            (#{item.itemId}, #{item.orderId}, #{item.quantity}, 
             #{item.productId}, #{item.unitPrice}, #{item.subtotal})
        </foreach>
    </insert>

</mapper>
